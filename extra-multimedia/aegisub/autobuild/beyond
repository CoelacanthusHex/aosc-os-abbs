abinfo 'Copying the automation scripts...'
mkdir -pv "$PKGDIR"/usr/share/aegisub/automation
cp -rv "$SRCDIR"/automation/{autoload,demos,include} \
       "$PKGDIR"/usr/share/aegisub/automation

abinfo 'Installing LICENSE...'
install -Dvm644 "$SRCDIR"/LICENSE "$PKGDIR"/usr/share/doc/aegisub/LICENSE
